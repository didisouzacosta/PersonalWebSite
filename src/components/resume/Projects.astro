---
const apps = [
	{ 
		name: "LoopSize",
		tags: ["iOS App", "Free"],
		imageUrl: "https://pub-81dc0daed73f4a358469301b9b80f493.r2.dev/apps/loopsize/loopsize-appicon.png",
		url: "/apps/loopsize"
	},
	{ 
		name: "CleanerXcode",
		tags: ["macOS App", "Free"],
		imageUrl: "https://github.com/didisouzacosta/CleanerXcode/raw/main/resources/app-icon.png",
		url: "https://github.com/didisouzacosta/CleanerXcode",
		target: "_blank"
	},
	{
		name: "Formidable",
		tags: ["Framework"],
		imageUrl: "https://github.com/didisouzacosta/Formidable/raw/main/app-icon.png",
		url: "https://github.com/didisouzacosta/Formidable",
		target: "_blank"
	}
]
---

<section id="projects">
    <h3 class="black">
        Projects
    </h3>

    <div class="apps">
        {
            apps.map((app) => {
                return <a href={app.url} target={app.target}>
                    <div class="app">
                        <img src={app.imageUrl} alt={app.name} loading="eager" class="app-icon"/>
                        
                        <h4 class="app-title black">
                            {app.name}
                        </h4>

                        <div class="tags">
                            {app.tags.map((tag) => {
                                return <span class="tag">
                                    {tag}
                                </span>
                            })}
                        </div>
                    </div>
                </a>
            })
        }
    </div>
</section>

<style>
    #projects {
		margin-top: 64px;
	}

	#projects .apps {
		display: grid;
		grid-template-columns: repeat(5, 1fr);
        grid-auto-rows: max-content;
		column-gap: 16px;
  		row-gap: 16px;
	}

	#projects .apps .app {
		text-align: center;
	}

    #projects .apps .app .app-icon {
		max-width: 100%;
        height: auto;
		object-fit: cover;
	}

	#projects .apps .app h4 {
		margin: 0px;
		padding: 0px;
	}

	.social-icon {
		width: 22px;
		height: 22px;
	}

	@media only screen and (max-width: 768px) {
		#projects .apps {
			grid-template-columns: repeat(2, 1fr);
			column-gap: 8px;
		}
	}
</style>